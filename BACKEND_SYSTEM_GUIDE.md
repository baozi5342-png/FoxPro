# FoxPro 后台管理系统 - 完整功能总结

## 项目概述

完整的加密货币交易平台后台管理系统，包含用户管理、交易控制、资金管理、身份认证、理财产品、借贷管理等全面功能。

**系统地址**: `http://localhost:3000/admin-system.html`
**管理员账号**: `admin` / `admin123`

---

## 功能模块详细说明

### 1️⃣ 仪表盘 (Dashboard)

**功能**:
- 实时统计总用户数、活跃交易数、待审核数、总资产
- 显示平台关键指标
- 支持数据刷新

**API端点**:
```
GET /api/admin/stats - 获取统计数据
```

---

### 2️⃣ 用户管理 (User Management)

**功能**:
- 查看所有注册用户列表
- 显示用户信息：用户名、邮箱、电话、注册时间
- 编辑用户基本信息
- 修改用户状态（激活/冻结/封禁）
- 删除用户账户

**API端点**:
```
GET    /api/admin/users                  - 获取所有用户列表
PUT    /api/admin/user/:id/status        - 修改用户状态
POST   /api/admin/user/:id/balance       - 修改用户余额
DELETE /api/admin/user/:id               - 删除用户
```

---

### 3️⃣ 身份认证管理 (Authentication)

#### 3.1 初级认证审核
- 查看用户提交的身份证信息
- 审核用户身份
- 批准或驳回认证
- 记录审核时间和状态

**API端点**:
```
GET  /api/admin/auth/primary                    - 获取初级认证待审核列表
POST /api/account/verification/primary          - 用户提交初级认证
```

#### 3.2 高级认证审核
- 查看用户提交的银行卡/资金证明
- 审核高级认证请求
- 批准或驳回请求
- 支持审核备注

**API端点**:
```
GET  /api/admin/auth/advanced                   - 获取高级认证待审核列表
POST /api/account/verification/advanced         - 用户提交高级认证
```

---

### 4️⃣ 秒合约管理 (Quick Contract)

#### 4.1 秒合约配置
**功能**:
- 配置交易时间周期（10秒-3600秒）
- 设置盈利比例（10%-200%）
- 配置最小/最大投资金额
- 保存多个配置版本
- 查看配置历史

**API端点**:
```
GET  /api/quick-contract/config                 - 获取秒合约配置
GET  /api/admin/quick-contract/config           - 获取管理员配置列表
POST /api/admin/quick-contract/config           - 保存新配置
PUT  /api/admin/quick-contract/config           - 更新配置
```

#### 4.2 秒合约交易控制 ⚠️ **核心功能**

**功能** - **管理员可以控制用户输赢**:
- 查看所有进行中的秒合约交易
- 手动设置用户交易的赢/输结果
- 支持实时干预交易结果
- 自动更新用户余额
- 显示交易详情：用户、币种、金额、方向、时间

**用途**:
```
举例：
- 用户 Alice 投注 $100 看涨 BTC
- 系统模拟：70% 成功率
- 管理员可介入：
  ✓ 设置为 赢 → Alice 获得 80% 盈利 = +$80
  ✓ 设置为 输 → Alice 亏损 5% = -$5
```

**API端点**:
```
GET  /api/admin/quick-contract/trades           - 获取待处理交易
POST /api/admin/quick-contract/trades/:id/set   - 设置交易结果
GET  /api/admin/quick-contract/orders           - 获取所有交易历史
```

---

### 5️⃣ 理财产品管理 (Wealth Management)

**功能**:
- 创建新的理财产品
- 配置产品参数：
  - 产品名称（如：30天保本计划、活期宝）
  - 投资周期（天数，0表示活期）
  - 年化收益率（%）
  - 最低投资金额
  - 最大投资金额
  - 风险等级（低/中/高）
  - 产品描述
- 启用/禁用产品
- 查看产品购买统计
- 删除产品

**内置产品示例**:
- 保本稳赚 - 30天 (8% 年化收益)
- 智选收益 - 90天 (12% 年化收益)
- 高收益计划 - 180天 (18% 年化收益)
- 活期宝 - 随时提取 (5% 年化收益)

**API端点**:
```
GET  /api/wealth/products                       - 获取产品列表
POST /api/admin/wealth/products                 - 创建产品
POST /api/wealth/purchase                       - 用户购买产品
GET  /api/wealth/orders                         - 用户理财订单
POST /api/wealth/redeem                         - 用户提取产品
POST /api/admin/wealth/toggle                   - 启用/禁用产品
```

**自动结算**:
- 到期产品自动结算
- 本金 + 收益自动返还用户账户
- 支持活期产品随时提取

---

### 6️⃣ 币种兑换管理 (Exchange Rate)

**功能**:
- 统计用户币种兑换情况
- 显示兑换成功率
- 记录兑换历史
- 分析用户偏好币种

**API端点**:
```
GET /api/admin/exchange-rates                   - 获取兑换统计
```

**显示信息**:
```
用户名 | 币种 | 兑换次数 | 成功率 | 时间
Alice | BTC | 12 | 75% | 2026-01-02
```

---

### 7️⃣ 用户余额管理 (Balance Control)

**功能** - **直接控制用户账户资金**:
- 选择用户查看当前余额
- 增加用户余额（如：奖励、补偿）
- 扣减用户余额（如：罚款、手续费）
- 记录操作原因和时间戳
- 实时更新用户资产

**使用场景**:
```
场景1：新用户注册奖励
- 新注册用户 Bob
- 增加 $100 作为注册奖励
- Bob 立即可用

场景2：风险用户处理
- 检测到用户 Charlie 风险交易
- 扣减 $50 作为罚款
- 余额实时更新
```

**API端点**:
```
GET  /api/admin/users                           - 获取用户列表
POST /api/admin/user/:id/balance                - 修改用户余额
```

---

### 8️⃣ 充值管理 (Recharge Management)

#### 8.1 配置充值币种

**功能**:
- 添加支持的充值币种（BTC、ETH、USDT、SOL等）
- 配置多个网络（TRC20、ERC20、Mainnet等）
- 设置收款钱包地址
- 生成二维码（自动）
- 配置最小/最大充值金额
- 启用/禁用币种

**内置币种**:
```
USDT (TRC20) → TLqwGzK7Yp2r2Lhk5jvGsRX9q3pYqRzK7U
USDT (ERC20) → 0x742d35Cc6634C0532925a3b844Bc9e7595f123b2
BTC → 1A1z7agoat4tDMjRL5FlVa8pQxXapNryQJ
ETH → 0x742d35Cc6634C0532925a3b844Bc9e7595f42bE
```

#### 8.2 充值订单审核

**功能**:
- 查看所有用户充值申请
- 显示订单状态：待审核、已批准、已驳回
- 批准充值 → 用户余额增加
- 驳回充值 → 提示用户重新上传
- 添加审核备注
- 支持 TX HASH 验证

**订单信息**:
```
用户名 | 币种 | 金额 | 状态 | TX哈希 | 操作时间
Alice | USDT | 100 | 待审核 | 0x123... | 2026-01-02 10:30
Bob | BTC | 0.5 | 已批准 | ... | 2026-01-01 15:45
```

**API端点**:
```
GET    /api/admin/recharge/coins                - 获取配置的币种
POST   /api/admin/recharge/coins                - 添加新币种
GET    /api/admin/recharge/orders               - 获取充值订单
PUT    /api/admin/recharge/orders/:id           - 更新订单状态
DELETE /api/admin/recharge/coins/:id            - 删除币种
```

---

### 9️⃣ 提现管理 (Withdraw Management)

**功能**:
- 查看所有用户提现申请
- 显示提现详情：币种、金额、提现地址、网络
- 订单状态跟踪：待处理、处理中、已完成、已失败
- 处理提现申请
- 支持多币种和多网络的提现

**API端点**:
```
GET /api/admin/withdraw/orders                  - 获取提现订单
PUT /api/admin/withdraw/orders/:id              - 更新订单状态
```

---

### 🔟 借贷管理 (Lending Management) ⚠️ **重要**

**功能** - **完整的借贷流程管理**:

#### 借贷申请
- 用户申请借贷（指定金额和期限）
- 显示申请金额、利率、期限
- 记录申请时间

#### 审核流程
- **批准借贷**:
  - 自动将申请金额转入用户账户
  - 用户可立即使用
  - 利息在期满时扣除
  - 自动生成还款计划

- **驳回借贷**:
  - 输入驳回原因
  - 用户可看到驳回通知
  - 可重新申请

**借贷参数**:
```
借贷金额：$100 - $1,000,000
利率：5% 年化（默认）
期限：7天 - 365天
```

**API端点**:
```
POST /api/lending/apply                         - 用户申请借贷
GET  /api/lending/applications                  - 用户借贷列表
GET  /api/admin/lending/applications            - 待审核借贷列表
POST /api/admin/lending/approve                 - 批准借贷
POST /api/admin/lending/reject                  - 驳回借贷
```

---

### 1️⃣1️⃣ 页面管理 (Page Management)

**功能**:
- 管理前端页面内容
- 支持的页面：
  - 首页 (Homepage)
  - 关于我们 (About)
  - 行情 (Market)
  - 交易 (Trading)
  - 资产 (Assets)
  - 账户 (Account)
  - 充值 (Recharge)
  - 提现 (Withdraw)
  - 客服 (Support)
  - 服务条款 (Terms)

- 编辑页面标题、描述、内容
- 支持 HTML 内容
- WYSIWYG 编辑器（可扩展）

**API端点**:
```
GET    /api/pages                               - 获取所有可管理页面
GET    /api/pages/:pageId/sections              - 获取页面区域
PUT    /api/admin/pages/:pageId/sections/:id    - 更新区域内容
POST   /api/admin/pages/:pageId/sections        - 创建新区域
DELETE /api/admin/pages/:pageId/sections/:id    - 删除区域
```

---

### 1️⃣2️⃣ 系统设置 (System Settings)

**功能**:
- 查看系统运行状态
- 数据库连接状态
- API 健康检查
- 系统诊断工具

**API端点**:
```
GET /api/system/db-tables                       - 获取数据库表列表
GET /api/system/users-count                     - 获取用户总数
GET /api/market/prices                          - 获取市场价格
```

---

## 核心特性

### ✅ 用户输赢控制
管理员可以完全控制用户在秒合约中的输赢结果：
- 查看进行中的交易
- 手动设置赢/输
- 自动更新余额和订单
- 显示详细操作日志

### ✅ 资金管理
直接管理用户账户资金：
- 增加余额（奖励、补偿）
- 扣减余额（罚款、手续费）
- 实时更新
- 操作记录

### ✅ 收益管理
控制平台收益分配：
- 设置秒合约盈利比例
- 管理理财产品收益率
- 配置借贷利率
- 实时统计

### ✅ 订单管理
完整的订单生命周期管理：
- 充值订单审核
- 提现订单处理
- 交易历史查看
- 订单状态跟踪

### ✅ 身份认证
用户身份验证流程：
- 初级认证（身份证）
- 高级认证（银行卡）
- 审核工作流
- 多级认证支持

### ✅ 借贷系统
完整的借贷管理：
- 申请管理
- 审核批准
- 驳回处理
- 自动转账
- 利息计算

### ✅ 理财产品
投资产品管理：
- 多种产品创建
- 自动结算系统
- 收益计算
- 期限管理

### ✅ 数据统计
实时数据分析：
- 用户统计
- 交易统计
- 资金流向
- 性能指标

---

## 数据库设计

### 核心表结构

```sql
users                          -- 用户账户
├── id, username, email, phone, password
├── country, status, isAdmin
└── createdAt, updatedAt

user_assets                    -- 用户资产余额
├── id, userId, balances (JSON)
└── createdAt, updatedAt

quick_contract_orders          -- 秒合约订单
├── id, userId, username
├── symbol, direction, seconds
├── amount, entryPrice, exitPrice
├── profit, profitRate, result
├── status, createdAt
└── updatedAt

quick_contract_config          -- 秒合约配置
├── id, seconds, profitRate
├── minAmount, maxAmount
├── enabled, createdAt
└── updatedAt

wealth_products                -- 理财产品
├── id, name, type, term
├── annualRate, minAmount, maxAmount
├── riskLevel, description
├── enabled, createdAt
└── updatedAt

wealth_orders                  -- 用户理财订单
├── id, userId, productId
├── amount, annualRate, term
├── status, expectedProfit, actualProfit
├── startDate, endDate
└── createdAt, updatedAt

recharge_orders                -- 充值订单
├── id, userId, username
├── coin, amount, network, address
├── txHash, status, notes
└── createdAt, updatedAt

withdraw_orders                -- 提现订单
├── id, userId, username
├── coin, amount, network, address
├── txHash, status, notes
└── createdAt, updatedAt

recharge_coins                 -- 充值币种配置
├── id, name, symbol, network
├── address, minAmount, maxAmount
├── status, description
└── createdAt, updatedAt

verification_submissions       -- 身份认证
├── id, userId, type
├── data (JSON), status
├── submittedAt, reviewedAt
├── reviewedBy, reviewNotes
└── createdAt, updatedAt

lending_applications           -- 借贷申请
├── id, userId, username
├── amount, rate, term
├── status, approvedAt, approvedBy
├── rejectionReason
└── createdAt, updatedAt

page_sections                  -- 页面内容
├── id, pageId, pageName
├── sectionKey, sectionTitle
├── sectionType, content, htmlContent
├── enabled, createdAt
└── updatedAt
```

---

## 安全性考虑

### 🔒 访问控制
- 仅管理员可访问后台系统
- Token-based 认证
- Session 管理
- 操作日志记录

### 🔒 数据保护
- SQLite 数据库加密（可选）
- 密码哈希存储（建议）
- 敏感数据脱敏
- 备份策略

### 🔒 操作审计
- 所有操作记录时间戳
- 记录操作人员
- 记录修改前后数据
- 异常操作告警

---

## 部署说明

### 系统要求
- Node.js 14+ 
- npm 6+
- SQLite 3+
- 2GB+ RAM
- 100MB+ 存储空间

### 启动服务器
```bash
cd c:\Users\admin\Desktop\foxpro
node server.js
```

### 访问地址
- 前端：http://localhost:3000/login.html
- 后台：http://localhost:3000/admin-system.html
- 文档：ADMIN_SYSTEM_README.md

### 默认账号
```
管理员账号：admin
管理员密码：admin123
```

---

## API 速查表

| 模块 | 方法 | 端点 | 说明 |
|------|------|------|------|
| 用户 | GET | /api/admin/users | 用户列表 |
| 用户 | PUT | /api/admin/user/:id/balance | 修改余额 |
| 秒合约 | GET | /api/admin/quick-contract/trades | 交易列表 |
| 秒合约 | GET | /api/admin/quick-contract/config | 获取配置 |
| 理财 | GET | /api/wealth/products | 产品列表 |
| 充值 | GET | /api/admin/recharge/orders | 充值订单 |
| 提现 | GET | /api/admin/withdraw/orders | 提现订单 |
| 借贷 | GET | /api/admin/lending/applications | 借贷列表 |
| 借贷 | POST | /api/admin/lending/approve | 批准借贷 |
| 认证 | GET | /api/admin/auth/primary | 初级认证 |
| 认证 | GET | /api/admin/auth/advanced | 高级认证 |

---

## 维护建议

### 日常维护
- 每日检查系统日志
- 监控数据库大小
- 检查磁盘空间
- 验证备份完整性

### 定期维护
- 每周备份数据库
- 清理过期日志
- 验证 API 正常运行
- 检查安全日志

### 月度维护
- 审计管理员操作
- 验证用户数据完整性
- 性能优化检查
- 安全更新检查

---

## 联系与支持

遇到问题？
1. 查看 ADMIN_SYSTEM_README.md 详细文档
2. 检查服务器日志：server.log
3. 验证数据库连接
4. 查看浏览器控制台错误

---

**系统版本**：1.0
**最后更新**：2026年1月2日
**开发者**：FoxPro Development Team
