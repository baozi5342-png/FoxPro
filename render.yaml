services:
  - type: web
    name: foxpro
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node server-production.js
    envVars:
      - key: NODE_ENV
        value: production

databases: []
